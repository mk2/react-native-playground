import React, { View, Text } from 'react-native';

const Control = React.createClass({
  render() {
    return (
      <View>
        <View></View>
      </View>
    );
  }
});

export default Control;
